# Gateway conventions

- TypeScript variables and functions use camelCase; types and classes use PascalCase.
- Filenames use kebab-case.
- Routes expose the consistent `{ success, message, data?, error? }` response shape.

'use client';

import { Button } from '../ui/button';
import Link from 'next/link';
import { useState } from 'react';
import { useAuth } from '../../features/auth/auth-context';
export function Header() { const { user, loading, signOut } = useAuth(); const [open, setOpen] = useState(false); return <header className="border-b border-border bg-background"><div className="mx-auto flex h-20 max-w-6xl items-center justify-between px-6 lg:px-8"><Link href="/" className="font-heading text-h3 text-primary">Meshly<span className="text-accent">.</span></Link><nav className="hidden items-center gap-7 text-small text-text-secondary md:flex"><Link href="/catalog" className="hover:text-primary">Catalog</Link><Link href="/cart" className="hover:text-primary">Cart</Link><Link href="/#about" className="hover:text-primary">About</Link>{!loading && (user ? <div className="relative"><button type="button" className="flex items-center gap-2 text-primary" onClick={() => setOpen((value) => !value)} aria-expanded={open}>{user.name || user.email}<span className="text-accent">⌄</span></button>{open && <div className="absolute right-0 top-10 z-10 w-44 rounded border border-border bg-surface p-2 shadow-subtle"><span className="block px-3 py-2 text-small text-text-secondary">Account coming soon</span><button type="button" className="block w-full rounded px-3 py-2 text-left text-small text-error hover:bg-background" onClick={() => { setOpen(false); void signOut(); }}>Sign out</button></div>}</div> : <Link href="/auth/sign-in"><Button size="sm" variant="secondary">Sign in</Button></Link>)}</nav>{!loading && (user ? <button type="button" className="md:hidden text-small text-primary" onClick={() => void signOut()}>Sign out</button> : <Link href="/auth/sign-in" className="md:hidden"><Button size="sm" variant="ghost">Sign in</Button></Link>)}</div></header>; }
